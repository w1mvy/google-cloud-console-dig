# google-cloud-console-dig

fuzzy-finder for Google Cloud Console Menu

**only mac support** (because use `open` command internally)

## Required

- `gcloud` command

## Usage

```
$ google-cloud-console-dig -p your-project-id
```
